type: external
# Paths to externally-computed coarse clustering. For GPU clustering prefer
# `clustering=gpu` (scripts/gpu_cluster.py), which records backend/params provenance;
# `external` is the generic raw-path ingest.
# centroids.npy: [K, dim] f32 ; assignments.npy: [n_tokens] u32, token i -> centroid.
# Token order must match TachiomIndex.add_documents_from_shards (sorted glob of the shard dir).
centroids_path: ???
assignments_path: ???
